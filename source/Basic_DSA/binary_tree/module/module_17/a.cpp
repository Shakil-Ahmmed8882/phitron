
#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    /*
        Problem → say 1-line what they want → Input/Output
        Constraints → int/long/limits
        Complexity → O1 / OlogN / On / On^2
        Data structure → which to use
        Problem type → array / graph / DP / greedy / math / simulation
        Approach → 2–3 steps plan
        Sample → dry run
        Edge cases → empty / min / max / negative / overflow
        Review → formulas / loop limits / index / off-by-one
*/

    return 0;
}