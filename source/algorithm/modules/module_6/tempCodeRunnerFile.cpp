r(int i = 0; i < e; i++){
    //     int a , b; 
    //     cin >> a >> b;
    //     adj_list[a].push_back(b);
    //     adj_list[b].push_back(a);
    // }

    // memset(vis, false, sizeof(vis));
    // cycle = false; 
    // for(int i = 0; i < n; i++){
    //     if(!vis[i])
    //         bfs(i); 
    // }