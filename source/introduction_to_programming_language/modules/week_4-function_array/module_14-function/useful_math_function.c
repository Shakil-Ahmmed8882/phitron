
#include <stdio.h>
#include <math.h>
int main()
{
     int ceilValue = ceil(6.8);
     int floorValue = floor(5.8);
     int roundValue = round(5.4);
     double squareValue = sqrt(20);
     int powerValue = pow(4,3);
     printf("powerValue = %d \n", powerValue);
     
    return 0;
}